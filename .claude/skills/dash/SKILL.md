# Dashboard Launcher

Launch the enhanced context window dashboard.

1. Check if `enhanced_dashboard.py` exists in the current directory
2. If not found, provide the path from the project root
3. Display the command to run:
   ```bash
   python3 enhanced_dashboard.py
   ```
4. Explain: "Run this in a separate terminal window to monitor your context usage in real-time"
5. Remind: "The dashboard will auto-detect your current Claude Code session"
6. Note: "Press Ctrl+C to stop the dashboard"
